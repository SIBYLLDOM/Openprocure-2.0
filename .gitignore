# This repo only tracks adminApp/, backend/, and SQL/ — everything else in
# this working directory (AUTOMATION SITE, erp_sql.sql, etc.) stays local.
/AUTOMATION SITE/
/erp_sql.sql

# Dependencies
node_modules/
adminApp/node_modules/
backend/node_modules/

# Build output
adminApp/dist/
adminApp/build/

# Secrets — never commit real credentials
backend/.env
adminApp/.env
.env
.env.local

# User-uploaded files (logos, certificates, catalogs, library/workspace docs)
backend/uploads/

# Logs
npm-debug.log*
*.log

# OS/editor
.DS_Store
Thumbs.db
.vscode/
